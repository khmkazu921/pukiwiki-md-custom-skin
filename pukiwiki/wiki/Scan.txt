#author("2022-12-04T05:10:53+00:00","","")
#notemd

# Scan

AC-test
:at-speed

DC-test
:logic

Scan-shift
:-

MBIST
:Memory Test


# What to see?

- Check if STA is correctly done
  - There are warnings that clocks are not supplied to MBIST-related circuit when function mode.
- `check_timing`
- `read_sdc.mbist.log`
  - `no driving cells` warinings are ignorable
- scan-reorder: `place.log`
  - Formality cannot be verified.
  - mode settings are required
- see `report_timing` to understand mbist / scan

# Commands

- セル同士を引き寄せる
``` tcl
create_bounds -region {{199 555} {248 135}} -effort high [all_fanout -endpoints_only -from [get_pins -of [get_cells {fmem/arbiterImpl/mem/*/*/*/inst4 fmem/arbiterImpl/mem/*/*/*/inst3}] -filter {direction==out}] -flat -only_cells]
```

!include(Top)
